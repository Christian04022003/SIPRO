{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",

    // **CLAVE:** Soluciona la mayoría de los errores de etiquetas
    "jsx": "react-jsx",

    // **CLAVE:** Soluciona los errores de etiquetas HTML y SVG
    "lib": ["ES2022", "DOM", "DOM.Iterable"],

    // **Añadido:** Muy recomendado para evitar errores de tipado de librerías externas (node_modules)
    "skipLibCheck": true,

    // Se elimina la línea "types": ["@svar-ui/react-gantt"] ya que estás usando Frappe Gantt.
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
